========================================
   QUICK FIX - PUT/DELETE API ERROR
========================================

The problem: You need to AUTHORIZE first!

Follow these 3 steps:

────────────────────────────────────────

STEP 1: Get a Token
────────────────────────────────────────
1. Go to: http://127.0.0.1:8000/docs
2. Find "POST /api/auth/register"
3. Click "Try it out"
4. Enter:
   {
     "email": "test@example.com",
     "password": "password123"
   }
5. Click "Execute"
6. COPY the "access_token" from response

────────────────────────────────────────

STEP 2: Authorize (MOST IMPORTANT!)
────────────────────────────────────────
1. Look at TOP RIGHT of page
2. Find the "Authorize" button 🔒
3. Click it
4. In the box, type:
   Bearer YOUR_TOKEN_HERE

   Example:
   Bearer eyJhbGciOiJIUzI1NiIsInR5...

5. Click "Authorize"
6. Click "Close"

────────────────────────────────────────

STEP 3: Now Use PUT/DELETE
────────────────────────────────────────
1. First create a task (POST /api/tasks/)
2. Then you can UPDATE (PUT /api/tasks/1)
3. And DELETE (DELETE /api/tasks/1)

All will work now! ✅

────────────────────────────────────────

⚠️ CRITICAL: You MUST do STEP 2!
   Without clicking "Authorize",
   PUT and DELETE will NOT work!

────────────────────────────────────────

Still not working?
Make sure:
✓ You typed "Bearer " (with space)
✓ You copied the FULL token
✓ You clicked the Authorize button

========================================
